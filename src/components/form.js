import React from 'react'

// styles
import '../styles/components/form.css'

export default function Form (props) {
    return (
        
        <div>
            {props.children}
        </div>

    )
}